# React 19 + Radix + eslint-config-next produce a peer graph npm's strict
# resolver backtracks on for minutes without ever failing. The tree it settles
# on is the same one legacy resolution produces, so skip the search.
legacy-peer-deps=true
fund=false
audit=false
